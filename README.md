# OracleDBHarness
 Explain/review explicitly supplied SQL files, pasted text, and exported plans; produce suggestions and patches | Local file access plus the applicable company policy for processing those files with the selected model
